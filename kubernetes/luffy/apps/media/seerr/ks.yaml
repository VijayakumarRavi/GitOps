---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &appname seerr
  namespace: flux-system
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *appname
  components:
    - ../../../../components/volsync
    - ../../../../components/kopiur
  dependsOn:
    - name: volsync
    - name: external-secrets-stores
    - name: crunchy-postgres-secretstore
    - name: envoy-gateway-config
    - name: kopiur
  interval: 30m
  path: "./kubernetes/luffy/apps/media/seerr/app"
  postBuild:
    substitute:
      APP: *appname
      KOPIUR_CAPACITY: 1Gi
      KOPIUR_CLAIM: seerr-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  timeout: 10m
  wait: false
