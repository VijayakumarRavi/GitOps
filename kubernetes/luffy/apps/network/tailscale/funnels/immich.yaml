apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: immich-funnel
  namespace: media
  annotations:
    tailscale.com/funnel: "true"
spec:
  defaultBackend:
    service:
      name: immich-server
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - immich
