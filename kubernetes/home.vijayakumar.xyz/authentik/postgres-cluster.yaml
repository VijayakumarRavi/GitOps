apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
  namespace: authentik
spec:
  instances: 3
  enableSuperuserAccess: true
  # bootstrap:
  #   initdb:
  #     database: authentik
  #     owner: authentik
  #     source: postgresql
  storage:
    size: 1Gi
