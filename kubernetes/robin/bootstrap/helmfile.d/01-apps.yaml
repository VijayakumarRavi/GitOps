---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile

helmDefaults:
  cleanupOnFail: true
  wait: true
  waitForJobs: true

releases:
  - name: cilium
    namespace: network
    chart: oci://quay.io/cilium/charts/cilium
    version: 1.19.5
    values:
      - templates/values_robin.yaml.gotmpl
    hooks:
      # Wait for Cilium CRDs before applying pool config
      - command: bash
        args:
          - -c
          - until kubectl get crd ciliumloadbalancerippools.cilium.io &>/dev/null; do sleep 5; done
        events:
          - postsync
        showlogs: true
      # Apply IPv6/IPv4 LB pool via postsync hook
      - command: kubectl
        args:
          - apply
          - --namespace=network
          - --server-side
          - --field-manager=kustomize-controller
          - --kustomize
          - ../../apps/network/cilium/config
        events:
          - postsync
        showlogs: true

  - name: coredns
    namespace: network
    chart: oci://ghcr.io/coredns/charts/coredns
    version: 1.46.0
    values:
      - templates/values_robin.yaml.gotmpl
    needs:
      - network/cilium

  - name: flux-operator
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
    version: 0.55.0
    values:
      - templates/values_robin.yaml.gotmpl
    needs:
      - network/coredns

  - name: flux-instance
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
    version: 0.55.0
    wait: false
    values:
      - templates/values_robin.yaml.gotmpl
    needs:
      - flux-system/flux-operator
