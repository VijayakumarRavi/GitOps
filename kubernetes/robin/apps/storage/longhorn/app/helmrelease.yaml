---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
spec:
  chart:
    spec:
      chart: longhorn
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: longhorn
        namespace: flux-system
      version: 1.10.1
  interval: 30m
  values:
    httproute:
      enabled: true
      parentRefs:
        - name: envoy
          namespace: network
      hostnames: longhorn.${CLUSTER_DOMAIN}
      path: /
    persistence:
      defaultClass: true
      defaultClassReplicaCount: &replica 1
      reclaimPolicy: Retain
      defaultDataLocality: best-effort
      recurringJobSelector:
        enable: false
      removeSnapshotsDuringFilesystemTrim: enabled
    preUpgradeChecker.jobEnabled: false
    metrics:
      serviceMonitor:
        enabled: false
    longhornUI:
      replicas: *replica
    csi:
      attacherReplicaCount: *replica
      provisionerReplicaCount: *replica
      resizerReplicaCount: *replica
      snapshotterReplicaCount: *replica
    defaultSettings:
      orphanAutoDeletion: true
      concurrentAutomaticEngineUpgradePerNodeLimit: 1
      defaultDataPath: /var/mnt/extra/longhorn
      defaultDataLocality: best-effort
      defaultLonghornStaticStorageClass: longhorn
      storageMinimalAvailablePercentage: 0
      storageReservedPercentageForDefaultDisk: 0
      nodeDownPodDeletionPolicy: delete-both-statefulset-and-deployment-pod
      allowRecurringJobWhileVolumeDetached: false
      autoCleanupSystemGeneratedSnapshot: true
      autoDeletePodWhenVolumeDetachedUnexpectedly: true
      disableSchedulingOnCordonedNode: true
      defaultReplicaCount: *replica
      engineReplicaTimeout: 8
      failedBackupTTL: 1440
      restoreVolumeRecurringJobs: true
      snapshotDataIntegrityImmediateCheckAfterSnapshotCreation: false
      snapshotDataIntegrityCronjob: 0 8 */7 * *
      upgradeChecker: false
      v2DataEngine: false
