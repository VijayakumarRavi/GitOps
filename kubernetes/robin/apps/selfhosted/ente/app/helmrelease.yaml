---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ente
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 30m
  timeout: 1m
  values:
    controllers:
      ente:
        annotations:
          reloader.stakater.com/auto: "true"
        replicas: 1
        containers:
          server:
            image:
              repository: ghcr.io/ente/server
              tag: latest@sha256:447547e9c4011d12b8e4e4a3a9c33a7828d9be791521024227b83f4b2888fafc
            env:
              - name: HOST
                value: "::"
              - name: ENTE_HTTP_HOST
                value: "::"
            envFrom:
              - secretRef:
                  name: ente-secret
              - secretRef:
                  name: ente-db-secret
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
          web:
            image:
              repository: ghcr.io/ente/web
              tag: latest@sha256:beed1bbf15b896a6a6ef7f1992602cd338cbfacd0f4b7f4b290680168c4014da
            env:
              - name: HOST
                value: "::"
              - name: HOSTNAME
                value: "::"
            envFrom:
              - secretRef:
                  name: ente-secret
              - secretRef:
                  name: ente-db-secret
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
    route:
      photos: &route
        hostnames:
          - photos.${CLUSTER_DOMAIN}

        parentRefs: &id001
          - name: envoy
            namespace: network
        rules:
          - backendRefs:
              - identifier: photos
                port: http
      api:
        <<: *route
        hostnames:
          - ente.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: api
                port: http

      accounts:
        <<: *route
        hostnames:
          - accounts.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: accounts
                port: http

      public-albums:
        <<: *route
        hostnames:
          - albums.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: public-albums
                port: http

      cast:
        <<: *route
        hostnames:
          - cast.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: cast
                port: http

      share:
        <<: *route
        hostnames:
          - share.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: share
                port: http

      embed:
        <<: *route
        hostnames:
          - embed.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: embed
                port: http

      auth:
        <<: *route
        hostnames:
          - ente-auth.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: auth
                port: http

      paste:
        <<: *route
        hostnames:
          - paste.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: paste
                port: http

      locker:
        <<: *route
        hostnames:
          - locker.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: locker
                port: http

      memories:
        <<: *route
        hostnames:
          - memories.${CLUSTER_DOMAIN}
        parentRefs: *id001
        rules:
          - backendRefs:
              - identifier: memories
                port: http

    service:
      api:
        controller: ente
        ports:
          http:
            port: 8080
      photos:
        controller: ente
        ports:
          http:
            port: 3000
      accounts:
        controller: ente
        ports:
          http:
            port: 3001
      public-albums:
        controller: ente
        ports:
          http:
            port: 3002
      cast:
        controller: ente
        ports:
          http:
            port: 3004
      share:
        controller: ente
        ports:
          http:
            port: 3005
      embed:
        controller: ente
        ports:
          http:
            port: 3006
      auth:
        controller: ente
        ports:
          http:
            port: 3003
      paste:
        controller: ente
        ports:
          http:
            port: 3008
      locker:
        controller: ente
        ports:
          http:
            port: 3009
      memories:
        controller: ente
        ports:
          http:
            port: 3010
