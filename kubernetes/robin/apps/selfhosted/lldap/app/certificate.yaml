apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${CLUSTER_DOMAIN}
spec:
  commonName: "*.${CLUSTER_DOMAIN}"
  dnsNames: ["*.${CLUSTER_DOMAIN}"]
  issuerRef:
    kind: ClusterIssuer
    name: zerossl-production
  secretName: ${CLUSTER_DOMAIN}-lldaps-tls
