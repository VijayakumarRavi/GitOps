pocket-id: cd /app && exec su-exec "$PUID:$PGID" /app/pocket-id
backup: echo "1 * * * * /restic.sh" > /crontab && exec su-exec "$PUID:$PGID" supercronic /crontab
cf_tunnel: exec su-exec "$PUID:$PGID" cloudflared tunnel --no-autoupdate run --token "$CF_TOKEN"